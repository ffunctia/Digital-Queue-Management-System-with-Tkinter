# Digital-Queue-Management-System-with-Tkinter
The main purpose of this system is to create a queuing system with a graphical user interface using Tkinter, by treating CSV files as if they were a database.
